name: santanbhai
on:
  workflow_dispatch:
env:
  Greetings: Namskaar
  
jobs:
  buildjob:
    runs-on: ubuntu-latest
    steps:
    -  name: runscript
       run: echo "Hell $Greetings Bhai Jaan"
    - name: HashiCorp - Setup Terraform
  uses: hashicorp/setup-terraform@v2.0.3

  
