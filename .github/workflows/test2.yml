name: santanbhai
on:
  workflow_dispatch:
env:
  Greetings: Namskaar
  
jobs:
  buildjob:
    runs-on: ubuntu-latest
    steps:
    - name: Print Greeting Message
  run: echo "Hello $Greetings Bhai Jaan"
  
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
    
    - name: Verify Terraform Installation
      run: terraform version
  
